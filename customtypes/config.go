package customtypes

// Config contains data necessary to configure the bot
type Config struct {
	// Token to use when connecting to the server
	Token string
	// Command prefix to use when detecting commands
	CommandPrefix string
}
